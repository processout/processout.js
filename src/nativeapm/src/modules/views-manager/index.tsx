import { Container } from '../../components';
import { FormView } from './views';

const AppView = () => {
  return (
    <Container>
      <FormView />
    </Container>
  );
};

export default AppView;
